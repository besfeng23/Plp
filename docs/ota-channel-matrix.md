# OTA Channel Matrix

Phase 1N is a planning-only matrix. No channel below is connected, no credentials are configured in the repo, and no inventory, reservation, or rate sync is live.

| Channel | Role | Credential requirement | Room mapping requirement | Rate mapping requirement | Availability sync | Reservation import | Conflict risk | Current Phase 1N status | Phase 2A readiness |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Agoda | OTA marketplace | Partner credentials must be stored outside the repo before any dry run. | Map PLP accommodation to Agoda room ID and room name. | Map Agoda rate plan ID and rate plan name. | Disabled; manual blocks and direct bookings must override. | Disabled; future imports must be review-first. | High | Planning only; not connected; credentials missing. | Needs credentials, mappings, dry-run scope, and conflict queue. |
| Airbnb | OTA/listing marketplace | Account/API access must be confirmed outside the repo. | Map PLP accommodation to Airbnb listing or room equivalent. | Map pricing/rate plan assumptions without changing PLP booking prices. | Disabled; calendar source of truth must be decided. | Disabled; future imports must be review-first. | High | Planning only; not connected; credentials missing. | Needs listing mapping, block precedence, and approval gate. |
| Expedia | OTA marketplace | Expedia connectivity credentials must be stored outside the repo. | Map PLP accommodation to Expedia room type. | Map Expedia rate plan identifiers. | Disabled; no push in Phase 1N. | Disabled; future imports must be review-first. | High | Planning only; not connected; credentials missing. | Needs connectivity path, mapping validation, and dry-run logs. |
| Booking.com | OTA marketplace | Booking.com connectivity credentials must be stored outside the repo. | Map PLP accommodation to Booking.com room type. | Map Booking.com rate plan identifiers. | Disabled; direct booking protection must be enforced. | Disabled; future imports must be review-first. | High | Planning only; not connected; credentials missing. | Needs credential owner, room/rate mapping, and conflict review. |
| Direct Website | First-party channel | Existing admin environment only; no OTA credential. | Internal accommodation names are the protected baseline. | Existing direct pricing must not be changed by OTA planning. | Current direct records remain protected; no OTA push. | Not applicable as an OTA import source. | Medium | Planning only; direct bookings remain source records. | Needs source-of-truth definition before external distribution. |
| Google Hotel Ads | Metasearch / ads distribution candidate | Future ads or feed credentials must be stored outside the repo. | Mapping depends on whether feed/distribution is used. | Rate feed rules are undefined in Phase 1N. | Disabled; no feed or live sync. | Disabled; not a reservation import source in Phase 1N. | Medium | Planning only; not connected; credentials missing. | Needs role definition and feed-vs-sync decision. |
| Trip.com | OTA marketplace | Trip.com partner credentials must be stored outside the repo. | Map PLP accommodation to Trip.com room type. | Map Trip.com rate plan identifiers. | Disabled; no push in Phase 1N. | Disabled; future imports must be review-first. | High | Planning only; not connected; credentials missing. | Needs partner access, mappings, and conflict escalation owner. |
