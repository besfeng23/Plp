# Admin v2 deployment retry

This marker exists to trigger a fresh Vercel Git deployment after the React Admin v2 integration.

Target admin entry:
- `/admin` → `admin.html`
- `admin.html` → `src/admin/main.jsx`
- `src/admin/AdminApp.jsx` contains the PLP Ops v2 shell.

